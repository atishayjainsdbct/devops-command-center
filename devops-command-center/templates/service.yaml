apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-command-center.fullname" . }}
  labels:
    {{- include "devops-command-center.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-command-center.selectorLabels" . | nindent 4 }}
