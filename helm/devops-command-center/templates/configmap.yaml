apiVersion: v1
kind: ConfigMap

metadata:
  name: {{ include "devops-command-center.fullname" . }}-config

data:
  ENVIRONMENT: {{ .Values.config.environment | quote }}
  LOG_LEVEL: {{ .Values.config.logLevel | quote }}
  REFRESH_INTERVAL: {{ .Values.config.refreshInterval | quote }}
